---
name: article-review
description: harukamuy.com の記事を、ファクトチェック → 「大ファン」による声のチェック → 統合の順で最終チェックする。記事が一通り完成し、あずきさんと一緒の修正が終わったあとの「最終チェック」で使う。「最終チェックして」「記事レビュー」「チェックして」と言われたときに起動する。
---

# 記事レビュー（最終チェック）

あずきさんの運用フローの **③** にあたる工程。

1. 記事を完成させる
2. あずきさんと一緒に修正する
3. **← ここ。ファクトチェック ＋ 大ファンのチェック**
4. あずきさんの最終チェック → プッシュ

---

## この体制になった理由（2026-08-15）

スペースXのロックアップ記事で、**A（数字）・B（反対の立場）・C（校正）・D（結局なにが言いたいの）の4名を8周**回した。結果はこうだった。

| | 見つけたもの | 副作用 |
|---|---|---|
| 機械チェック | 余分な `</div>`、`%` による太字崩れ | なし |
| ファクトチェック（**原典を渡した回のみ**） | 「48.4%は6月30日時点」「92%は原典にない」 | なし |
| 文章レビュー B・C・D ×8周 | ほぼゼロ | **本文が6,300字→8,084字に膨らみ、読みにくくなった** |

あずきさんの評価は「**3名チェックの前の方がシンプルで良い記事だった**」。
そこから1,800字を削り戻し、あずきさんの15往復ほどの指摘で仕上げた。

### なぜ壊れたか

1. **レビュアーは「指摘を出す役」なので、きれいな記事を渡されると指摘を作る。**
   5周目以降の「不合格」は理由がどんどん細かくなった。Cは「太字が24件壊れている」と報告したが、機械検証では**1件も壊れていなかった**。
2. **B・C・Dはあずきさんが誰かを知らない。** だから「もっと慎重に書け」「留保を足せ」という汎用的な指摘しか出せず、
   積み上げた結果、あずきさんではない誰かの文章になった。
3. **わたしがレビュアーの言葉を原典より優先した。** 「90%超と92%を統一しろ」という指摘に従って92%側に寄せたが、
   ロイター原文は「90%超」。**レビューが誤りを持ち込んだ。**

---

## 大原則（この3つを破らない）

### 1. 原典が勝つ

あずきさんが貼ってくださったニュース原文が唯一の基準。
**レビュアーの指摘が原典と食い違ったら、原典が勝つ。** 迷ったら原典を読み直す。原典を手元に持たずにレビューを回さない。

原典が会話の中にしかない場合は、**セッションのログから引き出して**エージェントのプロンプトに全文を貼る。

```bash
python3 - <<'EOF'
import json
p='<セッションのjsonlパス>'
for line in open(p,encoding='utf-8'):
    try: d=json.loads(line)
    except: continue
    if d.get('type')!='user': continue
    c=d.get('message',{}).get('content')
    t=c if isinstance(c,str) else ''.join(x.get('text','') for x in c if isinstance(x,dict))
    if '<記事のキーワード>' in t: print(t[:3000])
EOF
```

### 2. ループしない

**文章レビューは1回だけ。** 「合格するまで回す」はファクトチェックにだけ適用する。
2周目以降の文章レビューは、指摘を作り出すだけで記事を良くしない。

### 3. 足すな、削れ

**今回の damage は全部「足す」方向だった。**
各エージェントに「**改善案は『削る』『言い換える』に限定する。文を足す提案は禁止**」と明示的に書く。

例外は、**読者が事実を誤解する箇所**だけ。その場合も本文ではなく注記に落とす。

---

## チェックの3本立て

| | やること | 誰が |
|---|---|---|
| **① 機械チェック** | 太字の崩れ・HTML・スマホ表示 | わたしが必ず自動で |
| **② ファクトチェッカー** | 原典・過去記事との数字の突合**だけ** | サブエージェント1名・1回 |
| **③ 大ファン** | 「あずきはそう言わない」「一度も言っていない」 | サブエージェント1名・1回 |

②と③は**同時に1メッセージで**走らせる。


---

## 第1段階：ファクトチェック（自分でやる。サブエージェントに投げない）

対象記事のパスを受け取る。指定がなければ、直近に編集した `content/posts/*.md` を使う。

### 1-0. 期間と起点の妥当性（数値の検算より先にやる）

**個々の数値が全部合っていても、測る期間が偏っていれば結論ごと間違う。**
2026年8月の景気敏感株の記事で、17年リターンを**リーマンショックの底（2009年2月）から**測っていた。
深く落ちたものほど戻りが大きく出るので、景気敏感株が構造的に有利になっていた。
起点を暴落前に戻したら**勝者が入れ替わり、2章・3章・5章・7章とまとめを全面的に書き直すことになった**。
このとき数値の検算はすべて✓で、誤りは1つもなかった。**検算が通ることと、結論が正しいことは別。**

比較の記事では、文章レビューに進む前に次を確認する。

- **起点は暴落の底や天井になっていないか。** 「データがそろう最初の月」を無自覚に使うと、たいてい直近の暴落の底になる
- **起点を5〜10通りずらして、順位が入れ替わらないか。** 入れ替わるなら、その順位は結論の根拠にできない。
  記事に書けるのは「起点を動かしても残ったもの」だけ
- **選べる起点のなかで、いちばん自分に不利（数字が低く出る）ものを主軸にする。** そのうえで「なぜその起点か」を本文に書く。
  これを書かないと、記事が「起点で結論は作れる」と主張した瞬間、その刃が自分に返る
- **終点も1つしか使っていないことを自覚する。** 起点だけ疑って終点を疑わないのは非対称なので、注記で断る

### 1-0b. 比較の土俵（同じく先にやる）

差が出たとき、それが**中身の差ではなく測り方の差**でないかを潰す。

| 見落とし | 実例 |
|---|---|
| **手数料の差** | 業種別ETF（0.352%）とTOPIXのETF 1305（0.066%）で、**年0.29ポイントの差が最初からついていた**。「TOPIXが年0.6ポイント上」の半分がこれだった |
| **片側だけの除外・控除** | ディフェンシブから最下位業種（電力・ガス）を外して「実は互角」と書いた。景気敏感からも最下位を外さないと不公平。**外すなら両側で外して並べる** |
| **母集団の欠けが、どちら側に不利か** | データ不足で商社・卸売を除外 → いちばん伸びた業種なので**景気敏感に不利**。「除外した」だけでなく**「どちらに不利か」まで書く** |
| **実測値と合成値を並べる** | TOPIXの下落幅は実測、比率表の行は2つを混ぜた合成値。合成は深めに出ると自分で注記していたのに、その誤差より小さい2.0ポイント差で勝敗を語っていた |
| **等加重と時価総額加重** | 業種ETFの単純平均とTOPIXは持ち方が違う。差の全部が中身の差ではない |

### 1-1. 全数値の再計算

記事中の数値を洗い出し、**その場でデータを取り直して**照合する。一致しないものは全部報告する。

株価・為替データは Yahoo Finance chart API を使う。

```
https://query1.finance.yahoo.com/v8/finance/chart/{symbol}?range={range}&interval={interval}&events=div|split
```

**必ず守るデータの落とし穴（実際に踏んだもの）:**

| 落とし穴 | 対処 |
|---|---|
| **`JPY=X` の月次データ（`interval=1mo`）は使わない** | 毎年10月がまるごと欠落し、値そのものもずれている（2023-07末を146.10と返すが実際は142.32）。**必ず `interval=1d` を取り、各月の最終値を自分で拾う** |
| `range=max&interval=1d` は銘柄ごとに日付グリッドが崩れる | 長期は `interval=1mo`。ただし為替だけは上記のとおり日次 |
| **`1306.T`（TOPIX ETF）は10:1の分割がデータに記録されていない** | 2016年をまたぐ長期計算が壊れる。TOPIXは **`1348.T`（MAXIS TOPIX）** を使う |
| 日本株の月次と日次で adjclose の配当調整が食い違う | 全銘柄で **`interval=1mo` に統一**して比較する（混ぜない） |
| 上場廃止期間のある銘柄（JAL 2010〜2012 など） | 長期計算から除外し、除外したことと残った比率を記事に明記 |

計算方法の決めごと:
- リターン＝分配込み（`indicators.adjclose[0].adjclose`）、円建ては各時点の為替を掛ける
- リスク＝対数リターンの標本標準偏差（n−1）を年率化（月次なら ×√12、日次なら ×√252）
- **期間や間隔が違う数字を並べない。** 並べるなら「そろわない理由」を注記に書く

検算は「記事の値 → 再計算値 → ✓/✗」の表で出す。✗があれば**その時点で報告し、文章レビューには進まない**。

### 1-2. 表示値どうしの引き算が合うか（読者がやる検算）

**再計算が正しくても、記事に載っている数字どうしで引き算が合わないと読者は止まる。**
端数まで使った値で計算し、表示は丸めた値、というズレが原因。実際に3件見つかった。

| 記事の表示 | 読者の計算 | 実態 |
|---|---|---|
| 13.9% − 11.4% は「2.4ポイント」 | 2.5になる | 実測13.849%を13.9%に丸めていた。**13.8%が正しい** |
| 29.9% − 8.4% は「21.4」 | 21.5になる | 丸める前の値で引いていた |
| 1,315万円の「±15%」が1,513万円 | 1,512になる | 上限だけσ=15.05%、下限は15.0%で計算していた |

```bash
# 記事に出てくる引き算を全部書き出して、表示値どおりに合うか確かめる
node -e 'const c=[["2章カード",15.0,11.4,3.6],["3章カード",13.8,11.4,2.4]];
for(const[n,a,b,d]of c){const x=+(a-b).toFixed(1);console.log(`${n}: ${a}-${b}=${x} / 表記${d} ${x===d?"OK":"×"}`);}'
```

**掛け算も同じ。** 「±667万円」と「±1,334万円」が並ぶなら 667×2 が表示どおりになっているか。

### 1-3. 同じ言葉が2つの数字を指していないか

読者がいちばん混乱するのはここ。実際にあった例:

- 「**分散の効果**」が、3.6ポイント（単純な足し算との差）と2.4ポイント（置き換えたときの差）の両方に付いていた
- 「**1銘柄ずつの平均**」が、27.1%（金額で重みづけ）と25.8%（単純平均）の両方に付いていた

```bash
# 同じ数字が複数の呼び名を持っていないか／同じ呼び名が複数の数字を持っていないか
grep -no "分散の効果\|分散で消えたぶん\|揺れ幅\|ばらつき\|荒さ" 対象.md
```

**呼び分けるか、違いを1行で説明するか、どちらかにする。**
呼び分けたら、記事の全箇所でそろっているか grep で確認する。

### 1-4. 過去記事との突き合わせ

```bash
# 同じ事実の数字ゆれ（保有銘柄数・評価額・過去に出した利回りやリスク）
grep -rn "117銘柄\|1,315万\|5,831万" content/posts/*.md
# 関連記事リンクの実在とタイトル完全一致
grep -oE '\(/blog/[a-z0-9-]+\)' 対象.md | tr -d '()' | sed 's|/blog/||' | while read s; do
  [ -f "content/posts/$s.md" ] && grep -m1 '^title:' "content/posts/$s.md" || echo "❌ $s"; done
```

過去記事と食い違う主張がないかも見る（例：前回「答えは数ではなかった」と書いたのに今回「銘柄数のおかげ」と書く、など）。

### 1-5. 機械チェック

```bash
f=対象.md
grep -nE "——|？。|\?。|^[、。]" $f                      # 禁則（全角ダッシュ厳禁）
grep -n 'grid-template-columns' $f | grep -v 'keep-grid' # 表のkeep-grid漏れ
printf "%s:%s\n" "$(grep -o '<div' $f|wc -l)" "$(grep -o '</div>' $f|wc -l)"  # タグ整合
python3 -c "
import re;t=open('$f').read()
print('正直:',t.count('正直'))                            # 本文1回まで
print('非日本語:',re.findall(r'[Ѐ-ӿ가-힯฀-๿]+',t) or 'OK')"
```

---

## 第2段階：2名を同時に走らせる

Agentツールを **1メッセージで2つ** 呼ぶ。どちらも `run_in_background: true`。

### ② ファクトチェッカー

**渡すもの（全部必須）**

- 対象記事のパス
- 突合用の過去記事のパス（前回の関連記事）
- **原典の全文**（あずきさんが貼ったニュース本文をプロンプトに丸ごと貼る）

**プロンプトに必ず書くこと**

```
【最優先の基準】以下がこの記事の元になった報道の原文です。
ここにない数字を記事が使っていたら誤り。ここにある但し書きを記事が落としていて、
それが読者を誤解させるなら誤りです。

---原文ここから---
（全文を貼る）
---原文ここまで---

チェック観点（これだけ）:
1. 原文にない数字を使っていないか
2. 原文の時点情報（「○月○日時点」「○月に入り」など）が正しく反映されているか
3. 記事内の数字が本文・図解・まとめで一致しているか
4. 計算が合うか
5. 過去記事の数字と矛盾していないか
6. 読者が事実を誤解する箇所がないか
7. 出典が示されていない事実がないか

【やらないこと】
- 文章表現の good/bad、言い回しの好み、「もっと慎重に書け」という指摘は一切不要です。
- 構成・章立て・読みやすさの指摘も不要です。
- 留保を足せという提案は禁止です。

事実の誤りがゼロなら「合格」と書いてください。ファイルは絶対に編集しないこと。
```

### ③ 大ファン（読者代表）

**B・C・Dを置き換える役。** ブログを全部読んでいて、あずきさんの声を知っている読者。

**プロンプトに必ず書くこと**

```
あなたは harukamuy.com の大ファンです。あずきさんの記事を全部読んでいて、
書き方の癖も、これまで何を言ってきたかも知っています。

【先に読むもの（必須）】
1. /Users/shinyashiraki/Documents/harukamuy-blog/STRATEGY.md の
   「## 8. 文体・キャラクターのルール」（一人称・口調・文末・ごまもちの出し方）
2. content/posts/ から、今回と近いカテゴリの記事を **5本** 選んで読む
   （今回が news なら news を5本。`grep -l 'category: "news"' content/posts/*.md` で選ぶ）

読んだうえで「あずきさんはこう書く人だ」という像を自分の中に作ってから、対象記事に入ってください。

そのうえで対象記事を読み、次の観点だけで見てください。

1. 「あずきさんはそう言わない」— 152本のどこにも出てこない言い回し、
   キャラクターに合わない断定・言い訳・へりくだり
2. 「あずきさんならこう言う」— 同じ内容を、あずきさんの言葉に置き換える
3. 「それは一度も言っていない」— 過去記事と矛盾する主張、
   これまでの立場と食い違う書き方
4. 留保（「ただし」「とはかぎらない」「確かめようがない」）が多すぎないか。
   あずきさんは **言い切ってから1回だけ** 添える人です
5. 同じことを2回言っている箇所、カードで完結しているのに本文で言い直している箇所

【重要な制約】
- **改善案は「削る」「言い換える」に限定してください。文を足す提案は禁止です。**
- 「もっと慎重に書け」「留保を足せ」という指摘はしないでください。
  この記事は意図的に留保を減らしています。
- 事実・数字の指摘は不要です（別の担当が見ています）。
- 好みの範囲、任意レベルの提案では不合格にしないでください。
  **あずきさんの声として世に出せるかどうか**だけで判断してください。

出力形式: 指摘ごとに【該当箇所の原文引用】【なぜあずきさんらしくないか】
【過去記事での言い方（あれば引用）】【削る/言い換える案】

最後に必ず「合格」か「不合格」を書くこと。ファイルは絶対に編集しないこと。
```

**大ファンが今日いたら言えたはずのこと（プロンプトの例に使ってよい）**

- 「当てにいってないのに『外した』と書くのは、あずきさんらしくないです」
- 「留保が10回以上出てきます。あずきさんは言い切ってから1回だけ添える人です」
- 「『ゆっくり書く』『引いておく』は152本のどこにも出てきません」
- 「『動かなかったから偉かった、という話ではない』は言い訳が3段あります。あずきさんは1回しか言いません」

---

## 第3段階：統合する。ただし大ファンに拒否権がある

2名の指摘を1つの表に畳む。

```
| # | 誰 | 指摘 | わたしの判断 |
|---|----|------|-------------|
| 1 | ファクト | 48.4%は6月30日時点 | 採用：解除後の売りの根拠にできないので書き換える |
| 2 | 大ファン | 「ゆっくり書く」が不自然 | 採用：「順を追って書く」に |
| 3 | ファクト | 数字を統一しろ | **見送り：原典は「90%超」。原典が勝つ** |
```

### 大ファンの拒否権

**ファクトチェッカーの修正案でも、大ファンが「それを入れるとあずきさんの声じゃなくなる」と言ったら、大ファンが勝つ。**
その場合は「事実は守りつつ、あずきさんの言い方で書く」方法を自分で考える。両立しないときだけ、あずきさんに聞く。

### 反映の作法

- **必ず1パスで直す。** 正規表現をループさせない（`while True: subn()` は新しい崩れを作る）
- 直したら**その場で機械チェック**にかける（下記）
- **自分が直した箇所ほど疑う。** 今日、修正のたびに新しい太字崩れを持ち込んだ

---

## ① 機械チェック（毎回・自動で）

ローカルの dev server が動いている前提で、**レンダリング結果**に対して行う。ソースを数えるだけでは検出できない。

```bash
sleep 6 && curl -s http://localhost:3000/blog/SLUG > /tmp/x.html
python3 - <<'EOF'
import re
h=open('/tmp/x.html',encoding='utf-8').read()
b=re.search(r'<article.*?</article>',h,re.S).group()
t=re.sub(r'<[^>]+>','',b)
print('未変換**  :',len(re.findall(r'\*\*',h)))                      # 0でなければ崩れ
print('strong入れ子:',len(re.findall(r'<strong>[^<]{0,80}<strong>',h)))
print('閉じ重複  :',len(re.findall(r'</strong>[^<]{0,80}</strong>',h)))
print('div開閉   :',b.count('<div'),'/',b.count('</div>'))
print('全角ダッシュ:',t.count('——'))
print('本文字数  :',len(t))
sm=re.findall(r'summary-list.*?</ul>',b,re.S)
if sm: print('まとめ    :',sm[0].count('<li>'),'項目 /',len(re.sub(r'<[^>]+>','',sm[0])),'字')
EOF
```

**太字が壊れる条件（CommonMark の flanking）**

閉じ `**` の直前が句読点・記号（`。` `、` `）` `」` `%` `＋`）で、直後が文字のとき壊れる。
**`%` も句読点扱い**なので `持ち株は**48.4%**と報じられた` は壊れる。数字の多いこのブログでは頻出。

- **House rule:** 句点は太字の**外**に出す → `**〜だ**。次の文`
- **禁止:** 半角スペース回避（`**〜だ。** 次の文`）。日本語に空白が残る
- **`**` の数を数えるだけでは検出できない。** ペアがずれると全マーカーが消費され、
  リテラル `**` はゼロなのに `<strong>` の入れ子や `</strong>` の重複が起きる

**スマホ確認（375px）**

```js
(() => { const a=document.querySelector('article'); const t=a.innerText;
 return {overflow:document.documentElement.scrollWidth>innerWidth,
  stars:(t.match(/\*\*/g)||[]).length,
  cardsOK:[...a.querySelectorAll('.data-card')].every(x=>x.getBoundingClientRect().width<=345),
  matome:a.querySelectorAll('.summary-list li').length,
  readtime:document.body.innerText.match(/約\d+分/)?.[0]}; })()
```

---

## 本文とまとめのずれ（プッシュ前に必ず見る）

**本文を削ると、まとめに「本文にない主張」が残る。** 今日も3件あった。

```bash
# まとめの主張が本文にあるか、キーワードで確認する
grep -n '最高値\|次の解除\|動かさない' 対象.md
```

- まとめの各項目について、**本文のどこで言っているか**を指させるか確認する
- 指させないものは、本文に合わせるか、まとめから落とす
- 逆に、**記事の結論がまとめに入っていない**ことも起きる（章を書き直したとき）


---


---

## 長さのルール（全モード共通）

過去にHDV記事が6,977字→14,576字に膨らんだ。原因は情報が増えたことではなく、**留保が増えたこと**だった
（レビューを通した記事は「ただし」「ただ」「なお」が1,000字あたり2.0〜2.2回、通していない記事は0.4〜0.8回）。

| | 扱い |
|---|---|
| **情報**（読者が新しく得をすること） | **本文に足してよい。長くなってよい** |
| **留保**（「ただし〜の場合は違う」「〜とはかぎらない」） | **本文に足さない。注記・表の脚注・免責へ落とす** |

大ファンには「**改善案は「削る」「言い換える」に限定する。文を足す提案は禁止**」と指示する。
事実の誤解を防ぐためにどうしても足すなら、**本文ではなく注記に落とす**。

**大ファンのカット候補は3択で判断する**：削る／注記へ落とす／**残す**。残す場合は理由をあずきさんに報告する。

長さの管理は**読了時間**で見る（記事ページに表示される）。目安は15〜18分。全149記事の中央値は10分。

### 長すぎたときは、削るより分ける

**疲れる原因は長さではなく、主張の本数のことが多い。**
景気敏感株の記事は27分になり、削る作業をしても26分にしかならなかった。
削れる重複が640字しかなく、残りは全部が内容だったから。原因は別のところにあった。

- **数字の密度**：地の文1,000字あたり17.8個（他記事は10.3〜13.6個）。**覚える数値が88種類**あった
- **主張が3本**：①比率に最適点はない ②業種選択が報われない ③起点で結論が変わる。どれが本題か決まっていなかった

**打ち消し表現（ただし・でも・〜ではない）の密度は4.4回/千字で、他記事（3.3〜4.7）と変わらなかった。**
つまり犯人は留保ではなく数字と主張の本数。ここを取り違えると、削る作業が空振りする。

診断はこう出す（数えてから話す。感覚で「長い」と言わない）。

```bash
node -e '
const fs=require("fs");const md=fs.readFileSync(process.argv[1],"utf8").replace(/^---[\s\S]*?\n---\n/,"");
const s=t=>t.replace(/<[^>]+>/g,"").replace(/[#*_`>\-\[\]\(\)!]/g,"").replace(/\s/g,"").length;
const prose=md.replace(/<div class="data-card"[\s\S]*?\n<\/div>\n/g,"").replace(/<div class="conv-wrap">[\s\S]*?\n<\/div>\n\n<\/div>/g,"");
const n=(prose.match(/[−+]?\d+\.?\d*(%|ポイント|倍|万円)/g)||[]);
console.log("表示",Math.round(s(md)/500),"分／地の文",s(prose),"字／数字",n.length,"個・",new Set(n).size,"種類");
' content/posts/対象.md
```

**主張が2本以上あって20分を超えたら、削るのではなく前後編に分ける。**
情報を1つも捨てずに読みやすくできるのはこの方法だけ。`series` と `seriesOrder` を frontmatter に足し、
`lib/posts.ts` の `SERIES_NAMES` に表示名を1行追加すると、前後リンクと目次が自動で出る。
分けるときは各記事の主張を1本にし、つなぎ目（前編末の引き渡し／後編頭の要約3行）と、
またいだ章番号の参照（「次の章の表」など）を必ず直す。

---

---

## 公開済み記事モード

すでに公開している記事を見直すときは、上に加えて次を守る。

- **字数の上限は元の+20%まで。** 読者のためになる情報なら足してよい。ただし留保は本文ではなく注記へ。大ファンのカット候補で相殺できるならなお良い
- URLとタイトルは変えない（SEOの評価が振り出しに戻るため）
- excerptは**記事の外**に出る。記事内で定義した造語（「MY高配当株」など）は使わない
- 過去記事からの**引用文の中は置換しない**。呼び名を統一するときも、引用は当時の言葉のまま

### 検算の期間は「記事を書いた時点」に固定する

**「直近1年」は日々動く。** 今日そのまま再計算すると、記事の数字と合わなくて当然になる。
実際、公開時に11.4%だった数字が5日後には11.5%になっていた。

`range=1y` ではなく `period1` / `period2` で**公開日までに固定**して検算する。

```js
const P1=Math.floor(Date.parse("2025-07-20")/1000), P2=Math.floor(Date.parse("2026-08-10")/1000);
`https://query1.finance.yahoo.com/v8/finance/chart/${sym}?period1=${P1}&period2=${P2}&interval=1d`
```

そのうえで「記事の数字は**書いた時点では正しかった**」と確認できたら、それは誤りではない。直さない。

### 追記を入れるかどうか

記事末の「※YYYY年M月D日追記：〜を訂正しました」は、**読者の判断が変わる訂正のときだけ**入れる。

| 入れる | 入れない |
|---|---|
| 結論が変わる／金額の桁が変わる／事実関係が違っていた | 表示の丸めのズレ（1,513→1,512）、言い回しの修正、説明の追加 |

判断に迷ったらあずきさんに聞く。

---

## 周回の回し方

**文章レビュー（大ファン）は1回だけ。回さない。**
ファクトチェックだけは、✗が出たら直して**もう1回**かける。それでも✗が残るときはあずきさんに報告する。

**反映したら必ず再検算する。**
レビュー中に**わたし自身が持ち込んだ誤り**が、今日だけで複数あった（太字の崩れ、原典にない数字、余分な `</div>`）。
直した箇所は次のチェックで必ず 1-1〜1-3 と機械チェックにかけ直す。**新しく書いたものほど疑う。**

**「不合格」を額面どおりに受け取らない。**
エージェントは指摘を出す役なので、きれいな記事でも何か言う。
指摘を受けたら、**まず自分で検証する**。今日、Cの「太字24件崩れ」は機械検証で1件も崩れていなかった。
検証で否定できたものは、理由を添えて見送る。

---

## あずきさんに聞く場面

自分で決めずに **AskUserQuestion で聞く**。

| 場面 | 例 |
|---|---|
| **あずきさんの投資判断・立場に踏み込む提案** | 「117は必要なかったかもしれない、と書きますか」 |
| **記事の結論のトーンが変わる追加** | 「為替を除くと自分がいちばん荒い、という数字を入れますか」 |
| **レビュアーどうしの判断が割れた** | Cは「造語が浮く」、A・B・Dは「問題なし」 |
| **裏が取れない数字が記事にある** | 「日経平均VIの水準は確認できませんでした。落としますか」 |
| **本人の言葉づかい・呼び名** | 「MY高配当株」でいくか、和語にするか |

選択肢には**それぞれのプレビュー**（実際に入る文）を付ける。あずきさんは非エンジニアなので、
「どう変わるか」を文で見せたほうが決めやすい。

---
---

## 反映したあと

```bash
npm run build
```

ビルドは**修正のたびに必ず走らせる**（走らせないとプレビューに反映されず、「直っていない」となる）。
プレビュー確認は本番ビルドを配信する:

```bash
cd out && python3 -m http.server 4321
```

スマホ375pxで、表の列崩れと横はみ出しがないことを確認してから、あずきさんの最終チェックに回す。

## 書き方のルール

- あずきさんは非エンジニア。専門用語を避けて平易な日本語で
- 呼びかけは「あずきさん」
- アイキャッチ画像は明示指示があるまで Read も検索もしない
- `private/` と `data/` は絶対にコミットしない。push 前に必ず確認する:
  `git diff --cached --name-only | grep -iE "private|^data/"`